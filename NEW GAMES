<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Instant Win Portfolio</title>

<style>
/* =============================
   ROOT VARIABLES
============================= */
:root {
  --bg: #0b0e14;
  --panel: #121826;
  --accent: #00f0ff;
  --accent-soft: rgba(0,240,255,0.15);
  --text: #e6e9f0;
  --muted: #9aa3b2;
}

* { box-sizing: border-box; }
html { scroll-behavior: smooth; }

body {
  margin: 0;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Inter, Helvetica, Arial, sans-serif;
  background: linear-gradient(180deg, #070a10 0%, var(--bg) 40%);
  color: var(--text);
}

/* =============================
   TYPOGRAPHY
============================= */
h1 { font-size: 42px; margin-bottom: 16px; }
h2 { font-size: 28px; margin-bottom: 16px; }
h3 { font-size: 18px; min-height: 44px; }

p {
  color: var(--muted);
  line-height: 1.6;
  margin-bottom: 16px;
}

/* =============================
   LAYOUT
============================= */
section {
  max-width: 1200px;
  margin: 0 auto;
  padding: 80px 20px;
}

/* =============================
   HEADER
============================= */
.site-header {
  position: sticky;
  top: 0;
  z-index: 100;
  padding: 18px 28px;
  background: linear-gradient(
    180deg,
    rgba(11,14,20,0.95),
    rgba(11,14,20,0.6)
  );
  backdrop-filter: blur(8px);
  border-bottom: 1px solid rgba(255,255,255,0.06);
}

.brand-wrap {
  max-width: 1200px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  gap: 14px;
}

.brand-logo {
  height: 48px;
  display: block;
}

.brand-tagline {
  font-size: 12px;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--muted);
  border-left: 1px solid rgba(255,255,255,0.12);
  padding-left: 14px;
}

/* =============================
   BUTTONS
============================= */
.btn {
  padding: 14px 22px;
  border-radius: 8px;
  border: 1px solid var(--accent);
  color: var(--accent);
  background: linear-gradient(
    180deg,
    rgba(0,240,255,0.08),
    rgba(0,240,255,0.02)
  );
  text-decoration: none;
  font-weight: 600;
  display: inline-flex;
  justify-content: center;
  transition: all 0.2s ease;
}

.btn.primary {
  background: var(--accent);
  color: #000;
}

.btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 24px rgba(0,0,0,0.45);
}

/* =============================
   HERO
============================= */
.hero {
  text-align: center;
  padding-top: 120px;
}

.hero p {
  max-width: 720px;
  margin: 0 auto 32px;
}

.cta-row {
  display: flex;
  gap: 16px;
  justify-content: center;
  flex-wrap: wrap;
}

/* =============================
   PORTFOLIO GRID
============================= */
.games {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  gap: 24px;
}

.game-card {
  background: radial-gradient(120% 120% at 50% 0%, #162033 0%, #0e1322 60%);
  border: 1px solid #1f2a40;
  border-radius: 14px;
  padding: 18px;
  display: flex;
  flex-direction: column;
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}

.game-card:hover {
  transform: translateY(-6px);
  box-shadow: 0 12px 40px rgba(0,0,0,0.55);
}

.game-icon-wrap {
  height: 300px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 12px 0;
}

.game-icon {
  max-width: 100%;
  max-height: 100%;
  object-fit: contain;
  background: #0b1020;
  border-radius: 10px;
}

/* =============================
   SPECS
============================= */
.specs {
  margin-top: auto;
  display: grid;
  grid-template-columns: repeat(3,1fr);
  gap: 6px;
}

.specs div {
  background: #0b1020;
  border: 1px solid #1b2338;
  border-radius: 6px;
  padding: 6px;
  text-align: center;
  font-size: 13px;
}

/* =============================
   GAME DETAIL
============================= */
.game-detail {
  max-width: 1100px;
  margin: 120px auto;
  padding: 40px;
  background: rgba(18,24,38,0.85);
  border-radius: 20px;
}

.game-images {
  display: grid;
  grid-template-columns: 2fr 1fr;
  gap: 24px;
  margin: 40px 0;
}

.game-images img {
  width: 100%;
  height: auto;
  object-fit: contain;
  background: #0b1020;
  border-radius: 12px;
  display: block;
}

@media (max-width: 900px) {
  .game-images { grid-template-columns: 1fr; }
}
</style>
</head>

<body>

<header class="site-header">
  <div class="brand-wrap">

    <img src="./concept_gaming_logoFinal.png" alt="Concept Gaming" class="brand-logo">


    <span class="brand-tagline">Instant Win Supplier</span>
  </div>
</header>

<section class="hero">
  <h1>Ultra-Lightweight Instant Games</h1>
  <p>Modern instant-win games engineered for mobile-first play and high volatility.</p>
  <div class="cta-row">
    <a href="#portfolio" class="btn primary">View Portfolio</a>
    <a href="#contact" class="btn">Request Integration</a>
  </div>
</section>

<section id="portfolio">
  <h2>Game Portfolio</h2>

  <div class="games">
    <div class="game-card">
      <h3>Chain Reactor</h3>
      <div class="game-icon-wrap">
        <img src="./chain-reactor-icon.png" class="game-icon" alt="">
      </div>
      <p>Explosive cascading multipliers.</p>
      <a href="#chain-reactor" class="btn">View Game</a>
      <div class="specs"><div>High Vol</div><div>97% RTP</div><div>x10,000</div></div>
    </div>

    <div class="game-card">
      <h3>Neon Blackjack</h3>
      <div class="game-icon-wrap">
        <img src="./neon-blackjack-icon.png" class="game-icon" alt="">
      </div>
      <p>Instant single-hand blackjack.</p>
      <a href="#neon-blackjack" class="btn">View Game</a>
      <div class="specs"><div>High Vol</div><div>98% RTP</div></div>
    </div>
  </div>
</section>

<section id="contact">
  <h2>Partner With Us</h2>
  <p>info@concept-gaming.com</p>
</section>

<footer>
  © Instant Win Portfolio
</footer>

</body>
</html>

